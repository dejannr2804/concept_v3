# Backend
DJANGO_SECRET_KEY=test123
DJANGO_DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
FRONTEND_URL=http://localhost:3000

# Postgres
POSTGRES_DB=concept_v3
POSTGRES_USER=odoo18
POSTGRES_PASSWORD=odoo18
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Frontend
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# DigitalOcean Spaces (S3-compatible) configuration
# Required: your Space endpoint (region), bucket name, and credentials
SPACES_ENDPOINT_URL=https://nyc3.digitaloceanspaces.com
SPACES_BUCKET_NAME=your-space-name
SPACES_ACCESS_KEY_ID=your-access-key
SPACES_SECRET_ACCESS_KEY=your-secret-key

# Optional: region (used by boto3 client), public base URL (e.g., CDN), cache control
# SPACES_REGION=nyc3
# SPACES_PUBLIC_BASE_URL=https://cdn.example.com
# SPACES_CACHE_CONTROL=public, max-age=86400

# Root folder under which uploads go; product images will be stored at
# {SPACES_ROOT_FOLDER}/product_images/{generated_name}
SPACES_ROOT_FOLDER=uploads
